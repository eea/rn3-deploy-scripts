apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: reportnet3-data-claim
  namespace: reportnet
  labels:
    app: reportnet3-data-claim
spec:
  storageClassName: reportnet
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 200Gi