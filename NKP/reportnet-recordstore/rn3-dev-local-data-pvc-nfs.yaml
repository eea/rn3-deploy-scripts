apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: reportnet3-data
  namespace: reportnet
  labels:
    app: reportnet3-data-claim
spec:
  storageClassName: nfs-storage
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi