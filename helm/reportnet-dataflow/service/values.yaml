# Default values for reportnet.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# Specific Values for Dataflow

# Environment Variables
consul:
  server: consul
  port: 8500

kafka:
  bootstrap: bootstrap-kafka.reportnet.svc.cluster.local
  port: 9092

metabase:
    url: jdbc:postgresql://rn3-pg-helm-pgpool:5432/metabase
    user: dataflow
    password: password
    
zipkin:
    ip: zipkin-ui
    port: 9411

redis:
    ip: redis
    port: 6379
    sentinel: mymaster
    nodes: redis:26379
tagSufix: 
replicas: 1
version: 1.0.0
repo: eeacms
# Other Variables
